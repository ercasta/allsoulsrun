package engine

// func (t *Timeline) saveHistory(e Eventer, p EventSequencePhase) {
// 	if t.eventHistory == nil {
// 		t.eventHistory = make([]TimelineHistory, 10000)
// 	}

// 	t.eventHistory = append(t.eventHistory, TimelineHistory{t.CurrentSequence, p, e})

// }
